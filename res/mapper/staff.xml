<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="server.StaffServer">
    <select id="getAllStaff"  resultType="staff">
        select a.id,staff_name,staff_depart,b.typename,staff_salary from tb_staff a,tb_shoptype b
        where a.staff_shop_type=b.id;
    </select>
    <select id="getStaffByIdOrName" resultType="staff" parameterType="java.lang.String">
        select * from tb_staff  where tb_staff.staff_name like '%${key}%' or id like '%${key}%'
    </select>
</mapper>


